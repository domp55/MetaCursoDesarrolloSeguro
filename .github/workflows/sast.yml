name: 04 SAST

on:
  workflow_dispatch:
  workflow_call:

jobs:
  sast:
    name: Build & Static Security (CodeQL + SpotBugs)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
      checks: write
      packages: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      - name: Build with Maven
        run: mvn clean package -B -Dmaven.test.skip

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java"

      - name: Run SpotBugs (Maven)
        run: mvn com.github.spotbugs:spotbugs-maven-plugin:4.7.3.6:spotbugs -Dspotbugs.sarifOutput=true -Dspotbugs.xmlOutput=false -Dspotbugs.failOnError=false
        continue-on-error: true

      - name: Upload Spotbugs Report
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'target/spotbugs.sarif'
          category: 'spotbugs'
